# Generated by CMake at build time. Do not edit; do not commit the generated copy.
#
# This records what the binary in this wheel was actually compiled against, which
# is the only reliable way to explain a load failure. Without it, a user whose
# torch does not match gets `ImportError: undefined symbol:
# _ZN3c1017RegisterOperatorsE...` and no idea which wheel to install instead.
"""Build provenance for the compiled extension."""

from __future__ import annotations

from typing import Final

#: ``dynquant._version.KERNEL_ABI_VERSION`` this binary implements.
ABI_VERSION: Final[int] = @DYNQUANT_ABI_VERSION_PY@

#: Full torch version present at build time, e.g. ``"2.7.1+cu126"``.
TORCH_VERSION: Final[str] = "@DYNQUANT_TORCH_VERSION@"

#: CUDA version torch itself was built for, e.g. ``"12.6"``. Empty for CPU torch.
TORCH_CUDA_VERSION: Final[str] = "@DYNQUANT_TORCH_CUDA_VERSION@"

#: CUDA toolkit version that compiled this binary. Empty for a CPU-only build.
CUDA_VERSION: Final[str] = "@DYNQUANT_CUDA_VERSION@"

#: True when the extension contains device code.
WITH_CUDA: Final[bool] = bool(@DYNQUANT_WITH_CUDA_PY@)

#: ``CMAKE_CUDA_ARCHITECTURES`` as configured, e.g. ``"75-real;80-real;90-virtual"``.
CUDA_ARCHITECTURES: Final[str] = "@CMAKE_CUDA_ARCHITECTURES@"

#: Host compiler identification, for bug reports.
CXX_COMPILER: Final[str] = "@CMAKE_CXX_COMPILER_ID@ @CMAKE_CXX_COMPILER_VERSION@"

#: Build type. Anything other than ``Release`` in a published wheel is a mistake.
BUILD_TYPE: Final[str] = "@CMAKE_BUILD_TYPE@"
